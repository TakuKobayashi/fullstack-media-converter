# Cloudflare Workers Static Assets custom headers.
#
# Video conversion deliberately uses the single-thread @ffmpeg/core build
# from a CDN. Unlike @ffmpeg/core-mt, it does not use SharedArrayBuffer and
# therefore does not require COOP/COEP. Keeping the site non-isolated also
# avoids unnecessarily blocking cross-origin resources used by the app.
